<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.magee.generator.mapper.GenTableMapper">
    <select id="selectTable" resultType="com.magee.generator.vo.TableVO">
        SELECT table_name, table_comment FROM information_schema.tables WHERE table_schema = DATABASE()
    </select>
    <select id="selectColumn" resultType="com.magee.generator.vo.ColumnVO">
        SELECT column_name as name, IF(is_nullable = 'YES', 1, 0) as nullable, data_type , column_type , column_comment as comment, column_key  , character_maximum_length as max_length
        FROM information_schema.columns
        where table_name =#{tableName} and table_schema = DATABASE()
    </select>
</mapper>